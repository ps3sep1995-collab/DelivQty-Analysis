name: Daily Bhavcopy Scanner

on:
  schedule:
    - cron: '30 13 * * 1-5'  # सोमवार से शुक्रवार शाम 7:00 बजे IST
  workflow_dispatch:          # गिटहब UI से मैनुअल बटन द्वारा चलाने के लिए

jobs:
  run-scanner:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          pip install requests pandas numpy

      - name: Download Bhavcopy Data
        run: python 1_download_data.py

      - name: Commit and Push Changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add data/
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-update Bhavcopy & Scan Reports" && git push)
